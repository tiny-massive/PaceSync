// Secrets.template.swift
// ──────────────────────────────────────────────────────────────────
// SETUP: Copy this file to Secrets.swift and fill in the proxy values.
//        Secrets.swift is gitignored and must NEVER be committed.
//        The Anthropic API key is NOT stored here — it lives server-side in the
//        Netlify proxy (see proxy/README.md). The app only holds the proxy URL
//        and the shared app token that authorises it.
//
// enum Secrets {
//     static let proxyURL = "https://your-site.netlify.app/v1/messages"
//     static let appToken = "your-app-shared-token"
// }
// ──────────────────────────────────────────────────────────────────
